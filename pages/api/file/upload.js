
export default async function handler(req, res) {
    // const fs = require('fs')
    // const dialog = require('node-file-dialog')
    // const config = { type: 'open-file' }
    // dialog(config)
    //     .then(dir => res.status(200).send(dir))
    //     .catch(err => res.send(err))

}